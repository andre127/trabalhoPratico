<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="unigran.projeto.br.locaplus">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Principal"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CadastrarFuncionario" />
        <activity android:name=".CadastrarVeiculo" />
        <activity android:name="unigran.projeto.br.Listagem.ListarCliente" />
        <activity android:name="unigran.projeto.br.gerenciamentoLocacao.LocacaoRetirada" />
        <activity android:name="unigran.projeto.br.Listagem.ListarLocacao" />
        <activity android:name="unigran.projeto.br.Listagem.ListarVeiculo" />
        <activity android:name="unigran.projeto.br.gerenciamento.GerVeiculo" />
        <activity android:name=".CadastroCliente" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ListarCpfCliente"></activity>
    </application>

</manifest>