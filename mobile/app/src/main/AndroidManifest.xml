<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="unigran.projeto.br.locaplus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="unigran.projeto.br.gerenciamentoLocacao.GerenciamentoLocacao" />
        <activity android:name=".CadastrarCliente" />
        <activity android:name=".CadastrarFuncionario" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CadastrarVeiculo" />
        <activity android:name="unigran.projeto.br.Listagem.ListarCliente"></activity>
    </application>

</manifest>